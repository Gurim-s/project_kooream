<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.kooream.mapper.BrandCartMapper">
	<insert id="addCart">	<!--  이미지 등록 -->
			INSERT INTO Cart(Cart_no, M_no, p_no)
			VALUES(cart_no_seq.nextval, 1, #{p_no})
	</insert>
	<select id="brandCartList" resultType="com.kooream.domain.BrandCartVO">
		select c.cart_no, c.m_no, c.p_no,
     	p.P_NAME_EN, p.P_SIZE, p.P_BRAND
 		from Cart c
     		inner join PRODUCT p
         on c.p_no = p.p_no 
     		where c.m_no = #{m_no}
     		order by cart_no desc
     </select>


	
</mapper> 